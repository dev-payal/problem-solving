//Problem - Given an array of integers nums and an integer target, return indices of the two numbers such that they add up to target. You may assume that each input would have exactly one solution, and you may not use the same
element twice.You need to write a function which takes array of numbers as input and return output.

const Output = (array, target) => {
    var list = []

for (var i=0; i<array.length; i++){
    array.map((item) => {
    if(array[i]==item);
    else if(array[i]+item == target){
        list.push([i,array.indexOf(item)])
        }
    })
}
var result = list[0]
console.log(result)
}

// Time Complexity = O(2n^2)
